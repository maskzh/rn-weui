import { createAction } from 'redux-actions'

export const switchTab = createAction('switch tab')
export const setNavMapper = createAction('set navMapper')
export const setNavStyles = createAction('set navStyles')
